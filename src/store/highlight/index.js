const state = {
    msg: 'Hai Highlight'
};

const getters = {
    getMsg(state) {
        return state.msg;
    }
}

const actions = {
    //
}

const mutations = {
    //
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}